import { Meta } from '@storybook/addon-docs';

<Meta title="Modules/Ui.Typography" />

# Ui.Typography

For our Elm application, we capture all typography in a module called `Ui.Typography`:

```elm
example : Html msg
example =
    Ui.Typography.h1 "Hello, world!"
```
